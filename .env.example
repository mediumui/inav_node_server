/**
 * .env 示例文件
 * 
 * 复制此文件为 .env 并根据需要修改配置
 * 
 * 使用方法:
 *   PORT=8080 npm start
 */

# 服务器端口
PORT=3000

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# 启用CORS
CORS_ENABLED=true

# 允许的来源 (逗号分隔)
CORS_ORIGINS=*

# 请求体大小限制
BODY_SIZE_LIMIT=10mb
